[{
    "layer": "top", // Waybar at top layer
    "position": "top", // Waybar position (top|bottom|left|right)
    "output": ["DP-3"],
    "spacing": 0, // Gaps between modules (4px)
    "include": [
        "$HOME/.config/waybar/default-modules.json"
    ],
    // Choose the order of the modules
    "modules-left": [
        "custom/arch",
        "hyprland/workspaces",
        "custom/media",
	"cava"
    ],
    "modules-center": ["clock"],
    "modules-right": [
        "tray",
        "idle_inhibitor",
        "cpu",
        "memory",
        "temperature",
	"custom/gpu",
        "pulseaudio",
        "network",
        "custom/lock",
        "custom/quit",
        "custom/reboot",
        "custom/power"
    ]
},
{
    "layer": "top", // Waybar at top layer
    "position": "top", // Waybar position (top|bottom|left|right)
    "output": "HDMI-A-1",
    "spacing": 0, // Gaps between modules (4px)
    "include": [
        "$HOME/.config/waybar/default-modules.json"
    ],
    // Choose the order of the modules
    "modules-left": [
        "custom/arch",
        "hyprland/workspaces",
        "custom/media",
	"cava"
    ],
    "modules-center": ["clock"],
    "modules-right": [
        "idle_inhibitor",
        "pulseaudio",
        "network"
    ]
}
]

